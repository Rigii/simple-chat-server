# Simple Chat Server

The **_Simple Chat Server_** is a a backend monorepo setup created for demonstration and technology testing purposes.  
It implements REST APIs and Socket endpoints to provide communication and data storage for the **_Simple Chat_** multi-room frontend chat application.

Instructions for starting and running the application are located in the `start-serve-server.md` file in the `docs` directory.
